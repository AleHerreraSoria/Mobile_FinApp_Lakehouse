name: 'mobile_finapp_lakehouse'
version: '1.0.0'
config-version: 2

profile: 'mobile_finapp_lakehouse'

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  mobile_finapp_lakehouse:
    +materialized: table
